version: "3.8"

services:
  testserver:
    image: ${TESTSERVER_IMAGE:?TESTSERVER_IMAGE must be set to a digest-pinned obi-testimg rust reference}
    ports:
      - "${TEST_SERVICE_PORTS}"
    environment:
      LOG_LEVEL: DEBUG
    depends_on:
      otelcol:
        condition: service_started
