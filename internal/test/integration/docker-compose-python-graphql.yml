version: "3.8"

services:
  testserver:
    build:
      context: ../../../internal/test/integration/components/pythongraphql/
      dockerfile: Dockerfile
    image: hatest-testserver-python-graphql
    ports:
      - "${TEST_SERVICE_PORTS}"
    depends_on:
      otelcol:
        condition: service_started
