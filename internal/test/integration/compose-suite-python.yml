services:
  testserver:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/pythonserver/Dockerfile${TESTSERVER_DOCKERFILE_SUFFIX}
    image: hatest-testserver-python
    ports:
      - "${TEST_SERVICE_PORTS}"
      - 8999:8999
    depends_on:
      otelcol:
        condition: service_started
